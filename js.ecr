// This will be deprecated soon due to a new exciting scoping method me and yoct are working on :)
{
    window.document.scripts = _window.document.scripts;
    _window = undefined;

    <% response.body_io.gets_to_end %>
}